<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Nutrition Calculator</title>

  <meta name="description" content="Nuritional Calculator">
  <meta name="keywords" content="nutrition, calculator">
  <meta property="og:title" content="Nutrition Calculator">
  <meta property="og:type" content="article">
  <meta property="og:description" content="Nutrition Calculator">
  <meta property="og:url" content="https://jamesmonk.me">
  <meta property="og:image" content="https://jamesmonk.me/img/favicon.svg">

  <link rel="shortcut icon" href="/img/favicon.svg" type="image/svg+xml" />

  <style>
    input, select {
      all: unset;
      border-bottom: .125em purple solid;
      line-height: 1.25;
    }

    input {
      -moz-appearance: textfield;

      text-align: center;
      width: 4ch;
    }

    input::-webkit-outer-spin-button,
    input::-webkit-inner-spin-button {
      -webkit-appearance: none;

      margin: 0;
    }

    select {
      text-align: center;
    }

    div {
      background-color: purple;
      color: white;
      display: block;
      margin-top: 2.5ch;
      padding: 1ch;
    }
  </style>

  <link rel="stylesheet" href="/css/main.css">
</head>
<body>

<header>
  <h1>Nutrition calculator</h1>
</header>

<main>
  <form>
    <p>
      I am a biological
      <label id="label-sex" hidden>Sex</label>
      <select id="sex" onchange="calsPerDay()" value="0" aria-describedby="label-sex">
        <option value="male" selected>man</option>
        <option value="female">woman</option>
      </select>
      who is
      <label id="label-age" hidden>Age</label>
      <input id="age" type="number" onchange="calsPerDay()" placeholder="0" value="" aria-describedby="label-age">
      years old. I am
      <label id="label-height" hidden>Height (cm)</label>
      <input id="height" type="number" onchange="calsPerDay()" placeholder="0" value="" aria-describedby="label-height">cm tall
      and weigh
      <label id="label-weight" hidden>Weight (kg)</label>
      <input id="weight" type="number" onchange="calsPerDay()" placeholder="0" value="" aria-describedby="label-weight">kgs.
    </p>

    <p>
      I am
      <label id="label-physique" hidden>Physique</label>
      <select id="physique" type="text" onchange="calsPerDay()" value="0" aria-describedby="label-physique">
        <option value="2.6">shredded</option>
        <option value="2.3" selected>lean/athletic</option>
        <option value="2">carrying a little fat</option>
        <option value="1.8">obese</option>
      </select>
      and wish to
      <label id="label-goal" hidden>Goal</label>
      <select id="goal" type="text" onchange="calsPerDay()" value="0" aria-describedby="label-goal">
        <option value="0.8">lose fat</option>
        <option value="1" selected>maintain weight</option>
        <option value="1.1">gain muscle</option>
      </select>
      with a
      <label id="label-activity" hidden>Activity</label>
      <select id="activity" type="text" onchange="calsPerDay()" value="0" aria-describedby="label-activity">
        <option value="1.2">sedentary</option>
        <option value="1.375">light</option>
        <option value="1.55" selected>moderate</option>
        <option value="1.725">heavy</option>
        <option value="1.9">very heavy</option>
      </select>
      activity level.
    </p>

    <div>
      <p>You should aim for <strong><span id="totalCals">0</span> calories (kcals)</strong>, <strong><span id="totalProtein">0</span>g protein</strong>, <strong><span id="totalCarbs">0</span>g carbohydrates</strong> and <strong><span id="totalFats">0</span>g fats</strong> daily.</p>

      <p>You should eat around <strong><span id="totalFibre">0</span>g fibre</strong>, and limit upper intake to <strong><span id="totalSugar">0</span>g sugar</strong> and <strong><span id="totalSaturatedFats">0</span>g saturated fat</strong> for "optimal" health.</p>

      <p>Ideally, consume <strong>1-2g of Omega 3</strong> (DHA/EPA total), also <strong>drink plenty of fluids/water!</strong></p>
    </div>
  </form>
</main>

<footer>
  <small>Last update: 2025/08/02</small>
</footer>

<script>
  function calsPerDay() {
    const age = document.getElementById("age").value,
      height = document.getElementById("height").value,
      weight = document.getElementById("weight").value,
      activity = document.getElementById("activity").value,
      sex = document.getElementById("sex").value,
      goal = document.getElementById("goal").value;
      physique = document.getElementById("physique").value;

    let result;

    result = ((10 * weight) + (6.25 * height) - (5 * age) + ((sex == "male") ? 5 : -161)) * goal;

    const totalCals = document.getElementById("totalCals"),
      totalProtein = document.getElementById("totalProtein"),
      totalCarbs = document.getElementById("totalCarbs"),
      totalFibre = document.getElementById("totalFibre"),
      totalSugar = document.getElementById("totalSugar"),
      totalFats = document.getElementById("totalFats"),
      totalSaturatedFats = document.getElementById("totalSaturatedFats"),
      proteinMultiplier = 0.9,
      fatMultiplier = 0.25;

    const dailyCarbs = goal == "0.8" ? 
      Math.round(((activity * result) - (((weight * (physique)) * 4) + ((activity * result) * fatMultiplier))) / 4) : 
      Math.round(((activity * result) - (((weight * (physique * proteinMultiplier)) * 4) + ((activity * result) * fatMultiplier))) / 4);

    totalCals.innerHTML = Math.round(activity * result);
    totalProtein.innerHTML = Math.round(weight * (physique * (goal == "0.8" ? 1 : proteinMultiplier)));
    totalCarbs.innerHTML = dailyCarbs;
    totalFibre.innerHTML = Math.round(((activity * result) / 1000) * 14);
    totalSugar.innerHTML = Math.round(dailyCarbs * 0.1);
    totalFats.innerHTML = Math.round(((activity * result) * fatMultiplier) / 9);
    totalSaturatedFats.innerHTML = Math.round((((activity * result) * 0.1 ) / 9));
  }

  calsPerDay();
</script>
